# nodeLinker is left at its default, `pnp`: there is no node_modules directory
# anywhere in this project, and that is the point of the fixture.
enableGlobalCache: false

pnpUnpluggedFolder: .yarn/unplugged
